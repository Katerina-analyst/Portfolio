## 1. Описание и маппинг API-методов процесса онлайн-записи

### Метод: Получение списка категорий процедур
**Идемпотентность:** метод является идемпотентным и не изменяет данные в системе
**Лимит запросов:**  не более 60 запросов в минуту с одного IP-адреса. Лимит является общим для публичных REST API-методов чтения процесса онлайн-записи

**Маппинг ответа:**

| **Поле на фронте** | **Поле в JSON** | **Поле в БД** | **Таблица в БД** | **Комментарий** |
|---|---|---|---|---|
| Не отображается, используется для последующих запросов | `categoryId` | `id` | `procedure_categories` | Первичный ключ категории процедур |
| Название категории | `categoryName`| `category_name` | `procedure_categories` | - |


Сначала я определю следующее:

Метод: GET

Доступность:  Метод публичный, авторизация не требуется.

URL: /public/v1/procedures?categoryId={categoryId}&status=available`

Назначение метода: Получить список процедур из выбранной категории

Краткое описание результата(что возвращает метод): Возвращает список процедур из выбранной категории, отсортированный по названию в алфавитном порядке.

Идемпотентность:  метод является идемпотентным и не изменяет данные в системе

Лимит запросов: 60 в минуту

Заголовки: application/json и Retry-After: количество секунд до повторной попытки

Маппинг ответа:

**Маппинг ответа:**

| **Поле на фронте** | **Поле в JSON** | **Поле в БД** | **Таблица в БД** | **Комментарий** |
|---|---|---|---|---|
| Не отображается, используется для последующих запросов | `proceureId` | `id` | `procedures` | Первичный ключ процедуры |
| Не отображается, передается во входных параметрах | `categoryId`| `id` | `procedure_categories` | Внешний ключ на категорию процедуры |
| Название процедуры |procedureName| `procedure_name` | `procedures`| Отображается после выбора категории процедур |
| Описание процедуры | procedureDescription | description | 
| Длительность процедуры | 


Требовнаие к сортировке (также как и категории будет выпадающий список, а прайсе будут такие как таблички на экране, в заголовке название категории и ниже список процедур в категории. Всего 7 таких категорий и в каждой от 1 до 8 названий процедур)

Пример ответа